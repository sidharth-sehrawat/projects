apiVersion: v1
kind: Service
metadata:
  name: vote
  labels:
    app: vote

spec:
  type: {{ .Values.service.type }}

  selector:
    app: vote

  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
