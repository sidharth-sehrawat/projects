apiVersion: v1
kind: Service
metadata:
  name: result
  labels:
    app: result

spec:
  type: {{ .Values.service.type }}

  selector:
    app: result

  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
